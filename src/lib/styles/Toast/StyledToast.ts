import styled from 'styled-components';

const StyledToast = styled.div`
    position: fixed;
    z-index: 100000;
    top: 0;
    right: 0;
    display: flex;
    align-items: center;
`;

export { StyledToast };
