language: node_js

node_js:
  - 16.18.0

jobs:
  include:
    - stage: release
      node_js: 16.18.0
      deploy:
        provider: script
        skip_cleanup: true
        script:
          - npm run build && npx semantic-release